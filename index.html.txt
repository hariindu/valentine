<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Will You Be My Valentine ❤️</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #ff758c, #ff7eb3);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      color: white;
      text-align: center;
    }

    .card {
      background: rgba(255,255,255,0.15);
      padding: 25px;
      border-radius: 20px;
      width: 90%;
      max-width: 360px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
    }

    h1 {
      font-size: 22px;
      margin-bottom: 15px;
    }

    button {
      padding: 12px 20px;
      border: none;
      border-radius: 25px;
      background: #ff3366;
      color: white;
      font-size: 16px;
      cursor: pointer;
      margin: 6px;
    }

    button:hover {
      background: #ff1f5a;
    }

    .yes-no {
      display: none;
      margin-top: 10px;
    }

    .slider {
      display: none;
      margin-top: 15px;
    }

    .slider img {
      width: 100%;
      border-radius: 15px;
      animation: fade 1s ease-in-out;
    }

    @keyframes fade {
      from { opacity: 0.4; }
      to { opacity: 1; }
    }
  </style>
</head>

<body>

  <div class="card">
    <h1 id="title">Happy Lover’s Day 💖</h1>

    <button id="startBtn">Open My Heart 💌</button>

    <div class="yes-no" id="questionBox">
      <button onclick="sayYes()">Yes 💕</button>
      <button onclick="sayNo()">No 🙈</button>
    </div>

    <div class="slider" id="slider">
      <img id="slideImg" src="photo1.jpg" alt="Our Memories">
    </div>

    <audio id="song" src="song.mp3" loop></audio>
  </div>

  <script>
    const title = document.getElementById("title");
    const startBtn = document.getElementById("startBtn");
    const questionBox = document.getElementById("questionBox");
    const slider = document.getElementById("slider");
    const slideImg = document.getElementById("slideImg");
    const song = document.getElementById("song");

    const photos = [
      "photo1.jpg",
      "photo2.jpg",
      "photo3.jpg",
      "photo4.jpg"
    ];

    let index = 0;
    let interval;

    startBtn.onclick = () => {
      title.innerText = "Will you be my Valentine? 💘";
      startBtn.style.display = "none";
      questionBox.style.display = "block";
    };

    function sayYes() {
      title.innerText = "Our Beautiful Memories 💑";
      questionBox.style.display = "none";
      slider.style.display = "block";

      song.play(); // works on mobile after click

      interval = setInterval(() => {
        index = (index + 1) % photos.length;
        slideImg.src = photos[index];
      }, 3000); // auto slide every 3 seconds
    }

    function sayNo() {
      title.innerText = "I will wait forever ❤️";
    }
  </script>

</body>
</html>